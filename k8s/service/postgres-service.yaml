apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: hl-project
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None # headless service для statefulset
